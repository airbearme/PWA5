## 2025-05-15 - [Improving Accessibility for Interactive Lists]
**Learning:** Using semantic `button` elements for interactive list items (like location selection) provides immediate accessibility wins including keyboard navigation and screen reader support that non-semantic `div` elements lack.
**Action:** Always prefer `button` elements for interactive components, even when custom styling is required, and ensure they have proper focus-visible styles.
